const config = {
    secretPath: `${__dirname}/.secret/creds.csv`,
    settings: {
        headless: false,
        width: 1500,
        height: 764,
        oldWidth: 1920,
        oldHeight: 1080 
    }
}

export default config
